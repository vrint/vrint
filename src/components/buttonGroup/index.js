import VrButtonGroup from './buttonGroup'

/* istanbul ignore next */
VrButtonGroup.install = function install (Vue) {
  Vue.component(VrButtonGroup.name, VrButtonGroup)
}

export default VrButtonGroup
